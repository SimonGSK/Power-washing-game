{
  "name": "power-wash-co",
  "version": "1.0.0",
  "private": true,
  "description": "Power Wash Co. — a pixel-art power-washing business game in one HTML file",
  "scripts": {
    "build": "python3 tools/build.py",
    "check": "python3 tools/build.py --check",
    "icons": "python3 tools/icons_game.py",
    "test": "npm run build && playwright test",
    "test:headed": "npm run build && playwright test --headed",
    "sim": "node tools/sim.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
